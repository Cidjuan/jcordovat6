<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="jcordovat6.Views.vEstudiante"
             Title="vEstudiante">
    <VerticalStackLayout>
        <Label 
        Text="CONSUMO DE REST"
        FontSize="24"
        HorizontalTextAlignment="Center"/>

        <ScrollView>
            <Grid x:Name="gridEstudiantes">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
             
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Text="Código" FontAttributes="Bold" Padding="10" BackgroundColor="LightGray"/>
                <Label Grid.Row="0" Grid.Column="1" Text="Nombre" FontAttributes="Bold" Padding="10" BackgroundColor="LightGray"/>
                <Label Grid.Row="0" Grid.Column="2" Text="Apellido" FontAttributes="Bold" Padding="10" BackgroundColor="LightGray"/>
                <Label Grid.Row="0" Grid.Column="3" Text="Edad" FontAttributes="Bold" Padding="10" BackgroundColor="LightGray"/>
            </Grid>
        </ScrollView>

    </VerticalStackLayout>
</ContentPage>